import { CardSearchInputPipe } from './card-search-input.pipe';

describe('CardSearchInputPipe', () => {
  it('create an instance', () => {
    const pipe = new CardSearchInputPipe();
    expect(pipe).toBeTruthy();
  });
});
